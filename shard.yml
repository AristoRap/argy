name: argy
version: 0.1.0
description: Typed CLI framework for Crystal with nested commands, inherited flags, and lifecycle hooks.
authors:
  - Aristotelis Rapai <aristorap.dev@gmail.com>

targets:
  example:
    main: src/example.cr

crystal: ">= 1.20.0"

license: MIT
